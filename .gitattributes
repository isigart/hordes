# Force LF line endings everywhere — eliminates CRLF/LF noise between
# Windows (PowerShell) and Linux (Cowork sandbox) when running `git status`.
* text=auto eol=lf

# Binaries should never be normalised
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
*.woff binary
*.woff2 binary
